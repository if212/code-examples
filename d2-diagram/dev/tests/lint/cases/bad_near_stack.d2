# expect: E-node-overlap E-label-overlap
# Two objects on the same near constant: the second hides the first completely (looks fine in a PNG!).
a -> b
n1: First note {near: top-center}
n2: Second note {near: top-center}
