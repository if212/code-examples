# Before: a `||` in the code ends the `|ts` block early.
# expect-error: unexpected text after
...@neutral-theme
cfg: "config/retry.ts" {
  class: code-file
  src: |ts
    export const retries = Number(process.env.RETRIES) || 3;
  | {class: code}
}
