# Before: role classes but no theme import: nothing defines them, every box is d2's default.
# expect: S-src-cli-engine
direction: down
api: Orders API {class: [service; focal]}
db: "Orders DB\nPostgres" {class: datastore}
api -> db: SQL {class: dep}
