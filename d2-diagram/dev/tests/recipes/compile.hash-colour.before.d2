# Before: an unquoted hex colour: `#` starts a comment and swallows the value (and the closing brace).
# expect-error: missing value after colon
...@neutral-theme
direction: down
api: Orders API {class: service; style.font-color: #1E293B}
