# Before: an unquoted `{` in a label opens a map.
# expect-error: unexpected text after map
...@neutral-theme
direction: down
web: Web app {class: actor}
api: Users API {class: service}
web -> api: GET /users/{id} {class: dep}
