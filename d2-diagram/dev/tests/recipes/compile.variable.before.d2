# Before: an undefined ${var} (a typo, or the theme import is missing).
# expect-error: could not resolve variable
...@neutral-theme
direction: down
api: Orders API {class: service; style.fill: ${primary-60}}
