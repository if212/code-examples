# After: short English labels; the user's terms stay in the brief, as comments after the
# labels (product and tech names would stay as written).
...@neutral-theme
direction: right
app: Order service {class: service}
logs: User activity logs {class: datastore}
app -> logs: writes {class: dep}
