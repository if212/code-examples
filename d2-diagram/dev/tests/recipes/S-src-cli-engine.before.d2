# Before: no theme import and no layout-engine: d2 falls back to dagre (curved edges) and
# nothing carries a role class.
# brief: arch.brief
# expect: W-curved-edge
direction: right
classes: {tier: {width: 180}}
web: Web app
api: Orders API
db: "Orders DB\nPostgres" {shape: cylinder; class: tier}
events: "Order events\nKafka" {shape: queue; class: tier}
mailer: Mailer
web -> api: HTTPS
api -> db
api -> events: {style.stroke-dash: 4}
events -> mailer: {style.stroke-dash: 4}
