# Before: a theme code (B1, N2) used as a colour; they work only inside theme-overrides.
# expect-error: to be a valid named color
...@neutral-theme
direction: down
api: Orders API {class: service; style.fill: B1}
