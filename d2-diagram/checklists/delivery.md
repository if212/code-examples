# Delivery Checklist

- All requested formats rendered.
- Artifact paths returned.
- Commands run are documented.
- Any caveats noted (layout limits, viewer interactivity limits).
- Watch mode command provided if user iterates frequently.
- No artifacts beyond the requested formats; inspection PNGs from the visual loop are working files, not deliverables.
